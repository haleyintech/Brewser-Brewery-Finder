import { toast,Slide } from 'react-toastify';

export const toastOptions = {
    autoClose: 1500,
    position: toast.POSITION.BOTTOM_LEFT,
    transition: Slide,
    closeOnClick: true,
    closeButton: true,
  };